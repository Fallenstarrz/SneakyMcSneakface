using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Hitbox : MonoBehaviour {

    // This is a stub class, we were going to do a lot of things with this, but we ran out of time and had to scrap it.
    // It now only gives a common component between all pawns to search for when casting our ray.

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
